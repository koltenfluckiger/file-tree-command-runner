commands:
  - name: Generate Command File
    command:
      - |
        cat << 'EOF' > file-tree-command-runner.yaml
        commands:
          - name: Template Command
            command:
              - command -v ls
        EOF
